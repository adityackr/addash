/**
 * Checks if value is greater than or equal to other.
 * @param {*} value
 * @param {*} other
 * @returns {boolean}
 */

function gte(value, other) {
	return value >= other;
}

module.exports = gte;
